{
  "name": "demoqa-login-automation",
  "version": "1.0.0",
  "description": "AI-friendly Playwright framework for automating and verifying the DemoQA login flow with screenshot evidence and HTML reporting.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
